<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.sharksystem">
    <!-- AASPEngine needs to write in files -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:installLocation="preferExternal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:theme">

        <!-- tools:replace="android:theme" needed to import AASPAndroidService module -->
        <!-- should be android:launchMode="singleTask" -->



        <activity android:name=".persons.android.PersonListActivity" />
        <activity android:name=".makan.android.AddOpenMakanActivity" />
        <activity android:name=".makan.android.AddClosedMakanActivity" />
        <activity android:name=".radar.android.RadarActivity" android:launchMode="singleTask"/>
        <activity android:name=".makan.android.MakanViewActivity" />
        <activity android:name=".makan.android.MakanAddMessageActivity" />
        <activity
            android:name=".sharknet.android.settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".identity.android.IdentityActivity" android:launchMode="singleTask"/>
        <activity
            android:name=".makan.android.MakanListActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>